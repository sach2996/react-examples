// const environment = process.env.NODE_ENV || "development";

// const config = { backendUrl: "http://localhost:3000" };
const config = { backendUrl: "https://backend-cx7m.onrender.com" };

export default config;
